class Solution(object):
    def toLowerCase(self, str):
        """
        :type str: str
        :rtype: str
        """
        lower = ""
        for char in str:
            if(65<=ord(char)<=90):
                lower = lower + (chr(ord(char)+32))
                
            else:
                lower = lower + char
        
        return lower
